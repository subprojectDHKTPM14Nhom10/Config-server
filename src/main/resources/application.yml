server:
  port: 8888

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/subprojectDHKTPM14Nhom10/config-cloud.git
          default-label: main
          clone-on-start: true
        bootstrap: false